services:
  nginx:
    image: nginx:alpine
    network_mode: host
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - rest-server
      - web
      - cdn-server
      - websocket

  rest-server:
    build:
      context: .
      dockerfile: ./rest-server/Dockerfile
    network_mode: host
    volumes:
      - ${CDN_STORAGE_PATH}:/usr/src/app/storage
    depends_on:
      - postgres

  web:
    build:
      context: .
      dockerfile: ./web/Dockerfile
    network_mode: host

  cdn-server:
    build:
      context: .
      dockerfile: ./cdn-server/Dockerfile
    network_mode: host
    volumes:
      - ${CDN_STORAGE_PATH}:/usr/src/app/storage

  websocket:
    build:
      context: .
      dockerfile: ./websocket/Dockerfile
    network_mode: host
    depends_on:
      - postgres

  postgres:
    image: postgis/postgis:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: hortalink
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  pgdata: